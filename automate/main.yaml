---

 - name: Install portainer base
   hosts: all 
   tasks: 
    - name: apt install docker.io
      apt:
        name: "{{ item }}"
        state: present
        update_cache: yes
      loop:
      - docker.io
